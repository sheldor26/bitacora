---
name: Bug
about: Something does not work the way it says it does
labels: bug
---

**Command**

```
node .bitacora/cli.mjs ...
```

**Expected**

**Actual**

Paste the output, including any `doctor` errors.

**Environment**

- Node version (`node --version`):
- OS:
- Installed via: `npm create bitacora` / `npx create-bitacora` / cloned the repo
- Agent: Claude Code / Cursor / Codex / other / none

**Note:** `doctor` failing right after installation is not a bug. The template
ships with `bitacora:fill-me` placeholders and filling them in is step one.
